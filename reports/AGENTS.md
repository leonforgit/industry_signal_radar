<!-- codex-workspace-bootstrap:managed -->
# AGENTS.md

This directory usually contains editable source reports plus generated siblings.

## Default Rule

- Prefer editing source documents such as `.md`, `.csv`, or `.json` inputs.
- Regenerate `.html`, `.pdf`, `.json`, `.email.txt`, and `rendered/` outputs through the owning scripts instead of hand-editing them.
